# OpenClaw Device Activity Log

This is a sanitized static website describing OpenClaw activity performed in the
workspace. It intentionally excludes tokens, `.env` files, database contents,
private workspace instructions, and other secrets.

The site is plain HTML and CSS and is intended for GitHub Pages.
